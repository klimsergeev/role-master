# 🎭 Role Creator

Библиотека ролей для AI-агентов. Централизованное хранилище персон, инструкций и поведенческих паттернов для использования в разных проектах.

---

## Что это

**Role Creator** — проект для создания, хранения и публикации ролей AI-агентов. Роли описывают:

- 🎯 **Кто** агент (персона, экспертиза)
- 📋 **Что** делает (задачи, обязанности)
- 💬 **Как** общается (тон, стиль)
- ⚠️ **Чего не делает** (ограничения)

---

## Структура проекта

```
/Role Creator
├── /Roles              ← Исходники ролей (рабочая папка)
│   ├── /meta           # Мета-роли
│   ├── /assistants     # Помощники
│   ├── /specialists    # Специалисты
│   ├── /creative       # Креативные роли
│   └── /templates      # Шаблоны
│
├── /Production         ← Опубликованные роли (READ-ONLY)
│   └── README.md       # Каталог всех ролей
│
└── /scripts
    └── publish.sh      ← Скрипт публикации
```

---

## Как использовать

### Для агентов из других проектов

Читайте роли из папки `/Production`:

```
Твоя роль — UX Writer. Найди её описание в папке Production
```

Агент откроет `Production/README.md`, найдёт нужную роль в каталоге и прочитает инструкции.

### Для редактирования ролей

1. Редактируйте файлы в `/Roles`
2. Запустите скрипт публикации:
   ```bash
   ./scripts/publish.sh
   ```
3. Роли появятся в `/Production` с обновлённым каталогом

---

## Каталог ролей

📚 **[Production/README.md](Production/README.md)** — актуальный список всех ролей с описаниями

Каталог обновляется автоматически при запуске `publish.sh`.

---

## Правила

- **Production — только для чтения**. Агенты из других проектов не должны изменять эти файлы
- **Roles — рабочая папка**. Все изменения вносятся здесь
- **Публикация через скрипт**. Это гарантирует актуальность каталога в README

---

*Role Creator Project*

